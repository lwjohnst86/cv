---
title: Luke W. Johnston
---

```{r summary, child="_includes/summary.Rmd"}
```

-----

```{r, echo=FALSE, results='asis'}
library(yaml)
knitr::opts_chunk$set(echo = FALSE, results = 'asis')
sections <- yaml.load_file('_outline.yaml')$sections
names(sections) <- sapply(sections, function(x) x$title)
```

```{r}
# Choose which sections to include
# sections <- sections[c("Education", "Employment")]
for (s in sections) {
    options(restrict_to_year = NULL)
    cat('\n\n## ', s$title, '\n\n')
    source(s$source)
}
```
