#!/usr/bin/Rscript

rmarkdown::render_site()

# rmarkdown::render('index.Rmd', output_format = 'pdf_document',
#                    output_options = list(latex_engine = 'xelatex',
#                                          template = '_includes/style/style.tex'))
